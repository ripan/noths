<%= form_for(@misc_promotional_rule) do |f| %>
  <% if @misc_promotional_rule.errors.any? %>
    <div id="error_explanation" class="alert alert-danger">
      <h2><%= pluralize(@misc_promotional_rule.errors.count, "error") %> prohibited this misc_promotional_rule from being saved:</h2>

      <ul>
      <% @misc_promotional_rule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %>
    <%= f.select :name, options_for_select(ENTITY_NAMES, @misc_promotional_rule.name), {},  {:class=>"form-control"}%>
  </div>
  <div class="form-group">
    <%= f.label :discount_percentage %> %
    <%= f.select :discount_percentage, options_for_select((0..100).to_a, @misc_promotional_rule.discount_percentage.to_i), {},  {:class=>"form-control"}%>
  </div>
  <div class="form-group">
    <%= f.label :discounted_price %> £
    <%= f.text_field :discount_price, :class=>"form-control"%>
  </div>
  <div class="actions">
    <%= f.submit :class=>"btn btn-primary"%>
  </div>
<% end %>